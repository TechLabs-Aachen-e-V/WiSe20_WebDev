<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Event</title>

    <link href="/public/assets/index.css" rel="stylesheet" type="text/css" />
</head>

<body>
    <h1>Edit Event</h1>
    <form action="/events/<%= event._id %>?_method=PUT " method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" name="event[title]" id="title" value="<%= event.title %> ">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" name="event[location]" id="location" value="<%= event.location %> ">
        </div>
        <button>Update Event</button>
    </form>
    <a href="/events/<%= event._id %> ">Back to Event</a>
</body>

</html>